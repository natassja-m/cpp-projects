# Dynamic Arrays Project

This project demonstrates dynamic memory allocation in C for integers, doubles, and characters. Users can add values of each type, and the program prints all entries after each iteration.

Files:
- dynamic_arrays.c : Source code